import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "YourDramaClub - Discover Best KDramas & CDramas | Ratings, Reviews & Cast",
  description: "Explore the best Korean and Chinese dramas. Browse trending KDramas, CDramas, ratings, cast details, trailers, and more on YourDramaClub.",
  keywords: "kdrama, cdrama, korean drama, chinese drama, best kdrama 2026, drama ratings, drama cast",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
